requests>=2.31.0
slack-sdk>=3.27.0
playwright>=1.44.0
python-dotenv>=1.0.0
gdown>=5.0.0          # bajar carpetas publicas de Google Drive (multi-video)
flask>=3.0.0          # dashboard de administracion del bot (src/dashboard.py)

# Opcional — solo necesario si se activa GCS (GCS_BUCKET seteada). Instalar
# en la VM de GCP al migrar: pip install google-cloud-storage
# google-cloud-storage>=2.10.0
